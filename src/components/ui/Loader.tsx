import { LoaderWrapper, StyledLoader } from "./styles/Loader.styles";

const Loader = () => {
  return (
    <LoaderWrapper>
      <StyledLoader></StyledLoader>
    </LoaderWrapper>
  );
};

export default Loader;
